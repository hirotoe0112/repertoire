# ピアノ・レパートリー維持アプリ 設計書

合意日: 2026-08-14

## 1. 解くべき課題

**「レパートリー曲を練習するという意識が抜ける」**。弾けなくなること自体ではなく、弾けなくなる過程が意識に上らないことが問題。したがって本アプリの成否は「正確なスケジューリング」ではなく、**毎日ユーザーの目に情報が届くこと**で決まる。

## 2. 前提と非目標

| 前提 | |
|---|---|
| 利用者 | 一人専用 |
| 端末 | Android（PWAとしてインストール）／ Windows（通常のブラウザ） |
| ネットワーク | 常時接続前提。オフライン対応なし |
| 言語 | 日本語のみ |
| 配布 | ストア不使用。URL を開くだけ |

**非目標**（明示的に作らないもの）: 新曲の譜読み・仕上げの管理（Notion の領分）、複数ユーザー、録音の保存、楽譜PDFの保持、本番予定日からの逆算、FSRS 等の高度な記憶モデル、オフライン動作。

## 3. 技術構成

- **フロント**: Vite + React + TypeScript、UIライブラリなし（素のCSS）
- **ホスティング**: Cloudflare Pages（`*.pages.dev`）
- **API**: Cloudflare Workers
- **DB**: Cloudflare D1（SQLite）
- **通知**: Web Push（VAPID）＋ Workers Cron Triggers
- **PWA**: `manifest.json` ＋ 最小限の Service Worker（インストール可能化と Push 受信のため。キャッシュ戦略なし）
- **認証**: 合言葉1つ。初回入力後は端末に保存。サーバ側はハッシュ照合

## 4. データモデル

### pieces（曲）

| 列 | 内容 |
|---|---|
| `id` | 主キー |
| `composer` / `title` | 作曲者（任意）／曲名 |
| `status` | `人前で弾ける` / `復活中` / `休眠` ← **手で動かす** |
| `memorized` | 暗譜ラベル（真偽値。**アルゴリズムには影響しない**） |
| `interval_days` | 現在の間隔 |
| `ease` | 曲ごとの忘れやすさ係数（初期 1.0、範囲 0.5〜2.0） |
| `next_due` | 次回予定日（`人前で弾ける` のときのみ値を持つ） |
| `last_played_on` / `last_rating` | 最終記録日・最終評価 |
| `last_performed_on` | 最後に人前で弾いた日（記録のみ、任意入力） |
| `notes` | 自由メモ |

### sessions（記録）

`piece_id` / `played_on` / `rating`（◎○△×）/ `note`

### settings

1日上限・倍率・通知時刻・パスワードハッシュ 等

### push_subscriptions

端末ごとの Push 購読情報

## 5. 維持アルゴリズム

評価の意味は「**今日の演奏の結果**」ではなく「**いま、この曲がどれくらい弾ける状態か**」。部分練習だけの日でも判定でき、通し／部分の区別はデータとして持たない（必要ならメモに書く）。

| 評価 | 意味 | 間隔 | ease |
|---|---|---|---|
| ◎ | いま人前で通せる | × 2.5 | +0.05 |
| ○ | だいたい大丈夫、細部が不安 | × 1.7 | 据え置き |
| △ | 怪しい箇所がある | × 0.5 | −0.10 |
| × | 弾けなくなっている | **1日にリセット** | −0.15 |

`次の間隔 = clamp(現在の間隔 × 倍率 × ease, 1日, 180日)`

- **昇格時**: `休眠`/`復活中` → `人前で弾ける` に上げる際、その場で1回評価。基準3日から算出（◎→8日、○→5日、△→2日、×→1日）
- **維持状態（自動算出、手で触れない）**: `良好` = 予定日前 ／ `今日やる` = 予定日到来 ／ `要復習` = 超過日数が間隔を上回る、または直近評価が △・×
- **大幅超過しても自動降格しない**。状態変更は常に人間の手で

## 6. 画面（4つ）

### ① ホーム
これ以外を置かない。統計もグラフも置かない。

- 「復活中」の曲（固定表示）
- 「今日の維持練習」= 予定日到来分を、`要復習` 優先 → 超過日数の大きい順 → **上限5曲**。溢れた分は自動的に翌日へ繰り越し
- 各行に ◎○△× の4ボタン。**タップ1回で記録完了**

### ② 全曲一覧
状態フィルタ（人前で弾ける／復活中／休眠）、検索、次回予定日・最終演奏日でソート。`復活中` が3曲を超えると警告表示。

### ③ 曲詳細
メモ編集、評価履歴グラフ、暗譜ラベル、最終本番日、状態変更、次回予定日の手動上書き。

### ④ 設定
パスワード変更、1日上限、倍率、通知時刻、CSV取り込み、JSONエクスポート。

## 7. 通知（Web Push）

| いつ | 条件 | 文面 |
|---|---|---|
| 毎朝 **8:00 JST** | 対象1曲以上 | 「今日の維持練習: 3曲（ショパン バラード第1番 ほか）」 |
| 毎朝 **8:00 JST** | 対象0曲 | 「今日は維持練習なし。好きな曲を自由にどうぞ」 |
| 毎晩 **21:00 JST** | その日の記録が0件かつ対象あり | 「まだ記録がありません」 |
| **月1回**（朝の通知に同梱） | — | 「新しくレパートリー入りした曲はありませんか？」 |

Android で受け取るには、初回に Chrome でホーム画面へ追加し通知を許可する操作が必要。

## 8. 取り込みと書き出し

- **取り込み**: Notion で「仕上げた曲」に絞ったビューを CSV エクスポート → 設定画面のテキスト欄に貼り付け → **1行目のヘッダから「曲名の列」「作曲者の列」を選択** → 取り込み。**全曲 `休眠` として登録**され、スケジュールには一切乗らない。その後、今も弾ける曲だけを手で `人前で弾ける` に引き上げる
- **書き出し**: 全データの JSON エクスポート（バックアップ用）

## 9. 既定として決めた細部

- タイムゾーンは **JST 固定**
- 同じ曲は**1日1記録**（同日に押し直したら上書き）
- 記録直後に「取り消し」を数秒間表示（誤タップ対策）
- 曲の削除は確認ダイアログ付きの物理削除
- 倍率・上限・通知時刻はすべて設定画面から変更可能

## 10. 残るリスク

1. **甘め評価**（部分練習しかしていない日に ◎ を付ける）→ 間隔だけ伸びて静かに劣化。詳細画面の評価履歴グラフで自己補正する前提
2. **Notion との継ぎ目**（アプリはレパートリーのみを扱う分離構成）→ 仕上がった曲の登録漏れ。月1の問いかけ通知で緩和するが、完全には塞げない
3. **Cloudflare アカウントが未作成** → 最初の作業。`npx wrangler login` のブラウザ承認はユーザーの手で必要

## 11. 着手順

1. Cloudflare アカウント作成 → `npx wrangler login`（ユーザー操作）
2. D1 スキーマ作成、Workers の API と合言葉認証
3. React フロント（ホーム → 一覧 → 詳細 → 設定）
4. Pages へデプロイ、Android でインストール確認
5. CSV 取り込みで30曲を投入、うち2曲を `人前で弾ける` へ
6. Web Push（VAPID鍵、購読保存、Cron Trigger）

## 12. 未確定

- Notion の CSV 実物（列名）。設計 8 の列選択UIで吸収するため、着手の障害にはならない
